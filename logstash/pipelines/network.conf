# This pipeline has been merged into zeek_suricata_to_kafka_es.conf.
#
# Previously it consumed the "network-logs" Kafka topic and re-indexed
# Suricata alerts into campus-logs-*, duplicating both Kafka reads and
# Elasticsearch writes.  The primary pipeline now writes directly to
# campus-logs-* as a second ES output for security_alert events.
